# Slice 03 — persistent behavior seam (`regent-2mf0.3`)

Create one ShellLive, neutral semantic shell component, auth-neutral access context, public fixture content provider, supervised generation-tagged content coordinator, Ash-owned navigation/scroll/theme hooks, and a Design hook-composition seam. Do not create a product resource or policy yet because Phase 1 has no authenticated product state.

Acceptance: shell and document identity persist; latest destination wins; stale task results/exits are ignored; shell stays usable during load/error; history/top/local-state behavior is exact; Design cannot replace behavior hooks.
