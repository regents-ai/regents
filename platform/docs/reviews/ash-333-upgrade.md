# Ash 3.33.0 and platform gate verification

All four product platform gates passed on the current uncommitted checkouts. Regents' standalone identity library also passed. Work was completed directly by Astra after Sean asked to stop the Fable assignment; no new delegated assignment was started after that instruction.

## Changes

- Regents' 14 Credo findings are resolved without disabling checks. Snapshot comparison/error semantics remain intact; the in-memory showcase resource now has an explicit create policy rather than bypassing authorization.
- The homepage local-link assertion permits the existing `/llms.txt` endpoint. Browser assertions reflect the shared platform brand and compare actual colors rather than hex spelling; the Autolaunch source link uses its verified canonical repository URL.
- The fixed stylesheet's Sobelow finding has a documented, single-function `Traversal.SendFile` annotation; no broader scanner exception was added.
- All four platforms and standalone identity resolve Ash 3.33.0 and use `default_string_length_count: :codepoints`. Each Mix root was exercised directly: 80 ASCII codepoints are accepted, while one grapheme containing 81 codepoints is rejected by an 80-codepoint constraint.
- Autolaunch's upgrade was integrated with only Ash unlocked. Concurrent Patchbay and Techtree upgrades were preserved. Techtree's temporary dependency override was removed because shared identity now declares the compatible version itself.
- Gate follow-up: documented Autolaunch's direct historical-row test fixture. Corrected Techtree's catalog-rewrite helper to capture the old digest before replacing object bytes. The other in-flight Techtree fixture/assertion corrections were retained and verified, not overwritten. Historical proof bytes were not regenerated by this work.

## Executed results

| Component | Gate | Result |
| --- | --- | --- |
| Regents platform | `mix precommit` | 1,132 tests, 0 failures, 1 excluded |
| Regents identity | `mix check` | 16 tests, 0 failures |
| Autolaunch platform | `mix precommit` | 784 tests, 0 failures, 1 excluded |
| Patchbay platform | `mix precommit` | 490 tests, 0 failures |
| Techtree platform | `mix check` | 445 tests and 6 doctests, 0 failures |

Regents' existing homepage/showcase Playwright selection passed all 32 tests. TypeScript, 502 JavaScript tests and 2 asset-budget tests also passed. Autolaunch's TypeScript and 217 JavaScript tests passed; Patchbay's asset JavaScript test command passed. All three consumer asset builds passed. This does not claim that all 131 Regents browser tests were verified; the completed browser selection was homepage and showcase.

The Ash 3.33.0 outer checksum was checked against Hex: `a1b313eefd0e04aa626d4e54313aadec17f3d82422f75d45e66c2736b807bf00`. OSV's query for Hex/ash 3.33.0 returned no reported vulnerabilities. The release changelog names the CVE-2026-82752 fix.

## Reproduction and evidence

Tests used owned loopback PostgreSQL databases; database suites ran sequentially due to the local connection budget. Optional `SENTRY_DSN` was unset rather than supplied as an empty string. Regents' showcase diagnostic requires a 12-hex partition: the successful browser run used `_7f3a9c1e5b2d` on port 4002. Ordinary Regents tests used `_astra_831_review`; the other platforms used `_astra_831_ash_upgrade`.

Local execution logs and manifests:

- `/tmp/regents-ash-astra-precommit-final.log`
- `/tmp/regents-ash-astra-identity.log`
- `/tmp/regents-ash-astra-browser-final.log`
- `/tmp/regent-ash-consumer-verification-831/results.json` and its referenced logs
- `/tmp/regent-ash-runtime-checks-831.json`
- `/tmp/regent-ash-verified-versions-831.json`
- `/tmp/verify-regent-ash-consumers-831.py`

No files were staged, no commits were created, and nothing was pushed, deployed or published. Existing unrelated work remains in place. Local upgrade-preparation worktrees remain under `worktrees/{autolaunch,patchbay,techtree}/astra-ash-333-831`; their patches are already integrated or superseded by the retained concurrent upgrades.
