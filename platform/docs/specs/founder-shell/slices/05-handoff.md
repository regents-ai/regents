# Slice 05 — immutable Design handoff (`regent-2mf0.5`)

Independently review the complete Phase 1 diff. Generate an exhaustive ownership manifest from the staged file set and prove convergence. Commit only ash-platform Phase 1. Record SHA, route handoff digest, exact Design-owned paths, Ash-owned exclusions, theme interface, shell assigns/slots contract, and copy-paste acceptance commands. Do not copy the hero asset before the commit/handoff.

Acceptance: narrow immutable commit, clean worktree, all gates green, and an unambiguous section/path handoff that Design can implement without inferring product behavior.
