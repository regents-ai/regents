# Slice 02 — exact route catalog (`regent-2mf0.2`)

Implement the README's 15 exact routes and typed metadata in `AshPlatformWeb.RouteCatalog`. Derive router/catalog equality and Design handoff JSON/digest from that owner. Validate parameters before fixture/content loading and return 404 for invalid identifiers or reserved `nodes` tree slug.

Acceptance: exact allowlist and precedence, deterministic metadata, five Techtree roots, app-root behavior, invalid/deep-link tests, no extra route.
