# Reference Contracts

This folder keeps retired Autolaunch contract code for audit history only.

Current launch and deferred subjects use `src/revenue/RevenueShareSplitterV2.sol`.
Files here are outside the default Foundry source and test paths.
