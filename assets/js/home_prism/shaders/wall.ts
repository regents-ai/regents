// Generated file. Do not edit.
//
// Source: vercel-labs/vgpu @ bd3b05101fdd1193a1593558d3c52a0b2b18f31d
// Entry:  apps/docs/app/[lang]/(home)/components/prism-background/wall.wgsl
// Modules resolved into this shader:
//   @vgpu/wgsl-std/src/color/index.wgsl  sha256:9a65713c22469c7aea102286385e00d5ac8c5986bb415384af779c04fbf88401
//   apps/docs/app/[lang]/(home)/components/prism-background/scene.wgsl  sha256:3dd1f1aaf0d88dd6d87eabe49de36935398e00fb82422bc388ce83b406a0e3e9
//   apps/docs/app/[lang]/(home)/components/prism-background/wall.wgsl  sha256:0ba2f894e57dfd5f45b7e0b185b93b79a5d4fc5f80948a962cc107ab42c75c9c
// Method: @vgpu/wgsl 0.3.1 resolveShader(), with the same identifier-preserving
//   options the upstream bundler loader is configured with, then module paths rewritten
//   to the stable identities above. Resolution happens here, once, not in the browser.
export default {version: 1, wgsl: "// vgsl-module: apps/docs/app/[lang]/(home)/components/prism-background/wall.wgsl\n// The wall: a plane at z = 0, facing the camera, written in linear HDR.\n//\n// The wall uses the sRGB color selected in lil-gui wherever no light reaches it.\n// The light bundle is a separate sheet in front of this plane, composited between\n// the prism's back and front glass interfaces.\n\n     \n      \n\n@group(0) @binding(0) var<uniform> scene: _vgsl_bf23e165__Scene;\n\nstruct _vgsl_ee91aae7__VertexOut {\n  @builtin(position) position: vec4f,\n  @location(0) uv: vec2f,\n};\n\n/**\n * Two triangles covering the wall rectangle, wound counter-clockwise as the\n * camera sees them.\n *\n * There is no vertex buffer: the wall is defined by `scene.wallHalfExtent` and\n * the mapping in `scenePoint`, and reading those from the uniform block is what\n * keeps the plane in step with the texture when the canvas changes shape.\n */\n@vertex\nfn vs_main(@builtin(vertex_index) index: u32) -> _vgsl_ee91aae7__VertexOut {\n  var corners = array<vec2f, 6>(\n    vec2f(0.0, 1.0), vec2f(1.0, 1.0), vec2f(1.0, 0.0),\n    vec2f(0.0, 1.0), vec2f(1.0, 0.0), vec2f(0.0, 0.0),\n  );\n  let uv = corners[index];\n  var out: _vgsl_ee91aae7__VertexOut;\n  out.position = scene.viewProjection * vec4f(_vgsl_bf23e165__scenePoint(scene, uv), 0.0, 1.0);\n  out.uv = uv;\n  return out;\n}\n\n@fragment\nfn fs_main(in: _vgsl_ee91aae7__VertexOut) -> @location(0) vec4f {\n  let wallBase = select(_vgsl_4c2c8948__srgbToLinear3(scene.wallColor), vec3f(0.0), scene.causticOnly != 0u);\n  return vec4f(wallBase, 1.0);\n}\n\n// vgsl-module: @vgpu/wgsl-std/src/color/index.wgsl\n\n\n\n\nfn _vgsl_4c2c8948__srgbToLinear(value: f32) -> f32 {\n  if (value <= 0.04045) {\n    return value / 12.92;\n  }\n  return pow((value + 0.055) / 1.055, 2.4);\n}\n\nfn _vgsl_4c2c8948__srgbToLinear3(value: vec3f) -> vec3f {\n  return vec3f(_vgsl_4c2c8948__srgbToLinear(value.r), _vgsl_4c2c8948__srgbToLinear(value.g), _vgsl_4c2c8948__srgbToLinear(value.b));\n}\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n// vgsl-module: apps/docs/app/[lang]/(home)/components/prism-background/scene.wgsl\n// Uniforms shared by the wall and deterministic light-ribbon passes.\n\nstruct _vgsl_bf23e165__Scene {\n  viewProjection: mat4x4f,\n  wallHalfExtent: vec2f,\n  /** User-selected sRGB wall color; the wall pass linearizes it before lighting. */\n  wallColor: vec3f,\n  /** 1 shows only the generated light over black. */\n  causticOnly: u32,\n  /** World-space depth of the emissive sheet between the glass interfaces. */\n  lightPlaneZ: f32,\n  /** Fixed layout metadata used to decimate the debug wireframe. */\n  lightWhiteQuads: u32,\n  lightBeamSlices: u32,\n  /** User-controlled lateral and outgoing-distance falloff strengths. */\n  lightEdgeFalloff: f32,\n  rainbowFalloff: f32,\n}\n\n/** Maps top-origin texture coordinates to the wall plane in world space. */\nfn _vgsl_bf23e165__scenePoint(scene: _vgsl_bf23e165__Scene, uv: vec2f) -> vec2f {\n  return (uv - vec2f(0.5)) * vec2f(2.0, -2.0) * scene.wallHalfExtent;\n}\n"} as const
