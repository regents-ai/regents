# The local acceptance tasks do not load this file. Copy the value into the
# documented command explicitly so every disposable database has a unique name.
ASH_PLATFORM_ACCEPTANCE_RUN_ID=founder_local_001
